akka {
  loggers = ["akka.event.Logging$DefaultLogger"]
  loglevel = debug
  log-dead-letters = off

  actor {
    provider = "akka.cluster.ClusterActorRefProvider"
  }

  remote {
    log-remote-lifecycle-events = error
    netty.tcp {
      hostname = ${cluster.ip}
      port = 0
      bind-hostname = "0.0.0.0"
      bind-port = ""
    }
  }

  cluster {
    //DO NOT USE IN PRODUCTION!!
    auto-down-reachable-after = 5 seconds
    seed-nodes = [
      "akka.tcp://"${cluster.name}"@"${cluster.seed-ip}":"${cluster.seed-port}
    ]

  }

}

cluster {
  ip = "0.0.0.0"
  ip = ${?CLUSTER_IP}
  seed-ip = "0.0.0.0"
  seed-ip = ${?CLUSTER_IP}
  seed-ip = ${?SEED_PORT_1600_TCP_ADDR}
  seed-port = 2552
  seed-port = ${?SEED_PORT_1600_TCP_PORT}
  name = "AkkaClusterExample"
}

frontend {
  host = "127.0.0.1"
  port = 8080
}